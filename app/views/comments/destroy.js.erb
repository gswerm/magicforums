$('#flash-messages-container').html("<%=j render partial: 'partials/flash_messages' %>")

$('.comment[data-id=<%=@comment.id%>.id%>]').remove();

<% if @comment.persisted? %>
$('#comments-delete').replaceall("<%=j render partial: 'comment', locals: { comment: @comment, post: @comment.post, topic: @topic } %>")
<% end %>
